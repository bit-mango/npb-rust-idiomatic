[package]
name = "ep"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
rayon = "1.12.0"

[features]
default = ["lazy-parallel"]
S = []
A = []
B = []
eager = []
lazy = []
lazy-parallel = []
